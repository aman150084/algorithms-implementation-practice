#include<stdio.h>
struct node {
    struct node* next;
    int val;
    struct node* prev;
} snode;
snode* head = NULL;
snode* tail = NULL:
int main(){
    int n;
    scanf("%d", &n);
    
    printf("Hi there");
    return 0;
}